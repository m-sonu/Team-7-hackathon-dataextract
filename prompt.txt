Analyze the following Markdown content from a financial document.

Your task:
1. VENDOR IDENTIFICATION: Extract the business name from the top of the document.
2. BILL IDENTIFICATION (CRITICAL): 
   - Search for strings labeled 'No.', 'Invoice', 'Bill #', 'Receipt #', 'Inv#', or 'Serial'.
   - If no label exists, look for a standalone 4 to 10 digit number often found near the date or at the very bottom of the receipt (e.g., '000101' or '2779').
   - Map the most likely identifier to 'bill_id'.
3. TAX COMPLIANCE:
   - Search for a Tax/VAT/PAN registration number. 
   - Note: For Japanese bills, this starts with 'T' followed by 13 digits. 
   - Map this to 'vat_no'.
4. TOTALS: 
   - Locate the final amount due. Look for 'Total', '合計', or the largest number before the 'Cash/Change' section.
   - Map this to 'raw_total'.
5. ITEMS: Extract all line items and prices.

Respond ONLY with a valid JSON object:
{json_format}

Content to parse:
{markdown_content}